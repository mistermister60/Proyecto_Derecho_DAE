<?php

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;

/**
 * Test unitario de ejemplo generado por Laravel Breeze.
 *
 * Verifica que la configuración base de PHPUnit funcione correctamente.
 * Sirve como punto de partida para escribir tests unitarios adicionales.
 */
class ExampleTest extends TestCase
{
    /**
     * Verifica que la aserción básica `true === true` se cumple.
     *
     * Test de humo que confirma que PHPUnit está correctamente configurado
     * y que las pruebas unitarias pueden ejecutarse sin errores.
     */
    public function test_that_true_is_true(): void
    {
        $this->assertTrue(true);
    }
}
